---
all:
  hosts:
    localhost:
      ansible_connection: local
    ttd-nest:
      ansible_host: ttd-nest
      ansible_user: root
  children:
    build_hosts:
      hosts:
        ttd-edge:
          ansible_host: ttd-edge
          ansible_user: root
